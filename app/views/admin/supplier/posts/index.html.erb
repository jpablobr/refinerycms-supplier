<%= render :partial => '/admin/supplier/submenu' %>
<div id='records'>
  <% if searching? %>
    <h2><%= t('results_for', :scope => 'shared.admin.search', :query => params[:search]) %></h2>
    <% if @supplier_posts.any? %>
    <ul>
      <%= render :partial => "post",
                 :collection => @supplier_posts %>
    </ul>
    <% else %>
      <p><%= t('no_results', :scope => 'shared.admin.search') %></p>
    <% end %>
  <% else %>
    <% if @supplier_posts.any? %>
      <%= will_paginate @supplier_posts %>

      <%= render :partial => "sortable_list" %>

      <%= will_paginate @supplier_posts %>
    <% else %>
      <p>
        <strong>
          <%= t('.no_items_yet', :create => t('new', :scope => 'admin.supplier.submenu.posts')) %>
        </strong>
      </p>
    <% end %>
  <% end %>
</div>
