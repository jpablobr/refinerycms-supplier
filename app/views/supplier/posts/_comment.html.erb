<article class='supplier_comment_message' id='<%= "comment-#{comment.to_param}" %>'>
  <%= image_tag comment.avatar_url, :alt => comment.name, :class => 'avatar' %>
  <%= simple_format auto_link(comment.message.to_s) %>
  <footer class='supplier_comment_author'>
    <p>
      <%= t('by', :scope => 'supplier.posts.comments', :who => comment.name) %>,
      <%= t('time_ago', :scope => 'supplier.posts.comments', :time => time_ago_in_words(comment.created_at)) %>
    </p>
  </footer>
</article>
